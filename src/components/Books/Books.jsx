import React from 'react';

const Books = ({ id, book }) =>
{
    const id, book = {id, book}
    return (
        <div>
            console.log(id, book);
        </div>
    );
};

export default Books;